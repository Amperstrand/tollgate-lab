# Router-only environment — no ESP32 or phone required
# Usage: pytest --lg-env=environments/router-only.yaml tests/

targets:
  main:
    resources:
      NetworkService:
        address: "192.168.13.112"
        username: "root"
    drivers:
      SSHDriver: {}
